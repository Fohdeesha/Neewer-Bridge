# Rust build output
/target

# Reference projects (kept on disk for research, not part of this repo)
/NeewerLite
/NeewerLite-Python
# Decompiled NEEWER Studio APK + separate daemon project (151MB research material;
# our derived findings live in protocol-analysis.md and models.toml, which ARE tracked)
/from-bengt

# Large reference artifact
/artnet_protocol_specs.txt

# Local config holding real MACs (use config.example.toml as the template)
/config.toml
/neewer-bridge.toml

# OS / editor
.DS_Store
*.swp
