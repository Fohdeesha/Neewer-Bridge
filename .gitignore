# Rust build output
/target

# External references & other projects (kept on disk for research, NOT part of
# this repo). This folder holds several upstream projects that have their OWN git
# repos — NeewerLite/, NeewerLite-Python/, bengt-neewerd/ (verygeeky/neewer-lights),
# neewer-python/ — plus the decompiled NEEWER Studio APK/, Reference Docs/, and
# protocol-analysis.md. Ignoring the whole folder keeps those nested repos entirely
# untracked (never add/commit/push to them from here). Our own derived findings live
# in tracked files (models.toml, README.md), not in here.
/Analysis-Sources/

# Large reference artifact
/artnet_protocol_specs.txt

# `config.toml` is the tracked, ready-to-edit config shipped with the project —
# NOT ignored. (Legacy alternate name kept ignored in case it's used locally.)
/neewer-bridge.toml

# Local scratch / personal notes
/how2run.txt

# OS / editor
.DS_Store
*.swp
